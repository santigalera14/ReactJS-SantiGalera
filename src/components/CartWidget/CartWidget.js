import { cart } from '../../assets/cart.svg'


const CartWidget = () => {
    return (
        <div>
            <img src={cart.svg} alt="cart-widget"/>
            0
        </div>
    )
}

export default CartWidget